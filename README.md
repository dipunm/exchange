### This project uses `yarn` and `node` version 16.

## Usage

You will need multiple terminals:

First terminal:
```js
yarn grape
```

As many clients as you wish to run:
```js
node application.js
```

